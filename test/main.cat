fn main() -> int {
    int x;
    print("Enter a number: ");
    scan(x);
    print("You entered: %d\n", x);
    
        if (x < 10) {
        print("x is less than %d\n", 10);
    } else {
        print("x is not less than %d\n", 10);
    };
    
    return 0;
}
